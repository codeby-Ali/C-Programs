#include <stdio.h>
// Perimeter of a Square 

int main(){ 

    int side;
    printf ("Enter the side = ");

    scanf( "%d" , &side);
    printf("The perimeter of the square is = %d ", 4 * side);

    return 0;
}