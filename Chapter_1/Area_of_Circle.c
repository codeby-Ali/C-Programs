#include <stdio.h>
// Area of a circle 
int main() {
    float radius;
    printf (" Enter the Raduis = ");

    scanf("%f"  , &radius);

    printf ("Desired area is = %f",  3.14 * radius * radius );

    return 0;

}